# 多格式读取规范

按真实格式读取PDF、Office、图片和音视频，核对完整性。扫描PDF逐页渲染或OCR，关键金额、姓名、日期对照原图；OCR和机器转写不能冒充人工核验。

使用当前环境的对应工具，不安装开发者环境或写死可执行路径。本地失败不自动上传云端；上传须明确授权。原文件只读，提取稿与原件区分；复制时校验哈希，不覆盖已有原件。临时文件放系统临时目录，最终交付按用户指定位置。

保存到库内前先定位目标来源目录、查重并确认方案；知识页使用真实相对来源路径。实际读取范围不足时保留缺口，不称完整收录。
